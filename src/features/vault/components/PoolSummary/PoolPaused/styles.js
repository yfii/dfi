const styles = (theme) => ({
  container: {
    position: 'absolute',
    top: '0px',
    left: '0px',
    backgroundColor: 'rgba(0,0,0,0.15)',
  },
  texts: {
    padding: '5px 10px',
    fontSize: '14px',
    fontWeight: 'bold',
  },
});

export default styles;
